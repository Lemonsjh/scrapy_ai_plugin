{
  "name": "atlas-collector",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel dev",
    "test": "pnpm --filter @atlas/shared build && pnpm --filter @atlas/shared test && pnpm --filter @atlas/api test && pnpm --filter @atlas/extension test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
